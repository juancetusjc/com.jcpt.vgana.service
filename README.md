## Iniciando Proyecto de servicio

* Iniciar la BD Apache Derby
  * Creamos la instancia de la BDs Apache Derby
     > $cd $DERBY_INSTALL/bin
     > $ setNetworkServerCP
  * Iniciamos la BD
    > java -jar derbyrun.jar server start 
